wish =

could mark resources all for deletion declaratively

e.g., wrap with `del(token)` => which will apply the `.[SymbolGlobal('declastruct.delete')]='ASAP'` attribute to the object

the, declastruct would invoke the dao.delete operation for each resource that has that declaration
