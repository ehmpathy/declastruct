emit your response into .behaviors/v2025_11_21.contract/5.1.execution.phase6.v1.[feedback].v2.[taken].by_robot.md

=====

bootup your mechanic's role via `npx rhachet roles boot --repo ehmpathy --role mechanic`

then respond to the feedback below


=====

# blocker.1

 pull the version here from package.json
      .version('1.0.0');

ref: src/contract/cli/invoke.ts

# blocker.2

dont add a `resource:$class` prefix to resource identifiers in the output; `$slug` alone is sufficient,  as it does the right thing already


# blocker.3

dont spread DemoResource to clone them. just use .clone() directly
