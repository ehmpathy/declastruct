emit your response into .behaviors/v2025_11_21.contract/5.1.execution.phase3.v1.[feedback].v4.[taken].by_robot.md


=====

bootup your mechanic's role via `npx rhachet roles boot --repo ehmpathy --role mechanic`

then respond to the feedback below

=====


# blocker.1

we collocate tests;

remove src/__test__/verification/phase1.test.ts

put the tests into files scoped _per_ file

e.g.,


  describe('1.1 IsoTimestamp', () => {
    it('should be a string type', () => {
      // type verification
      const timestamp: IsoTimestamp = '2025-11-22T10:30:00.000Z';
      expect(typeof timestamp).toBe('string');
    });
  });

  goes into

src/domain.objects/IsoTimestamp.test.ts


etc



============


# blocker.2

import `HasMetadata` from 'domain-objects'

```

/**
 * .what = resource instance with persistence metadata
 * .why = represents a resource that has been persisted and has metadata properties
 */
export type HasMetadata<T> = T & {
  id?: number;
  uuid?: string;
  createdAt?: Date;
  updatedAt?: Date;
  effectiveAt?: Date;
};

```

should not be declared on its own

if you dont know how to use something, lookup the docs
