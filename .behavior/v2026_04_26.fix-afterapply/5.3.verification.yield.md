# verification yield

## verification checklist

### behavior coverage

| behavior (from wish) | test file | status |
|---------------------|-----------|--------|
| show apply command after plan | `asApplyCommandFromArgv.test.ts` | ✅ |
| transform same command as plan | `asApplyCommandFromArgv.test.ts` (14 cases) | ✅ |
| preserve caller's flags | `asApplyCommandFromArgv.test.ts` case 7 | ✅ |
| scope to --wish and --into | `asApplyCommandFromArgv.test.ts` cases 1, 8 | ✅ |
| edge cases (empty argv, unknown path, minimal input) | `asApplyCommandFromArgv.test.ts` cases 11-14 | ✅ |

### zero skips verified

- [x] no `.skip()` or `.only()` found (grep verified)
- [x] no silent credential bypasses (grep verified)
- [x] no prior failures carried forward

### tests executed — with proof

| test suite | command run | result | proof |
|------------|-------------|--------|-------|
| types | `rhx git.repo.test --what types` | ✅ | exit 0, passed (3s) |
| lint | `rhx git.repo.test --what lint` | ✅ | exit 0, passed (2s) |
| format | `rhx git.repo.test --what format` | ✅ | exit 0, passed (1s) |
| unit | `rhx git.repo.test --what unit` | ✅ | exit 0, 14 passed, 0 failed, 0 skipped |
| integration | `rhx git.repo.test --what integration` | ✅ | exit 0, 38 passed, 0 failed, 0 skipped |

### snapshot coverage for contract outputs

| contract | output variants | snapshot file | status |
|----------|-----------------|---------------|--------|
| plan CLI | success output | `plan.integration.test.ts.snap` | ✅ extant |
| apply CLI | success output | N/A (no new snapshots needed) | ✅ |
| asApplyCommandFromArgv | all 14 output variants | `asApplyCommandFromArgv.test.ts.snap` | ✅ new |

**note on transformer vs CLI distinction:**

the `asApplyCommandFromArgv` function is a **pure transformer** (not a CLI command). its outputs ARE the contract:
- 14 test cases cover all invocation patterns (npx, pnpm, yarn, bare, node_modules)
- 4 edge cases cover minimal/empty/unknown inputs
- all outputs are snapped in `asApplyCommandFromArgv.test.ts.snap`

the transformer has no error paths — it always returns a command string. there are no "negative paths" because the function doesn't validate or reject inputs.

the apply hint log message (`🥥 did you know?`) is ephemeral output, not a contract. log format is intentionally flexible.

### snapshot change rationalization

no `.snap` files were modified by this PR. the extant snapshots remain unchanged.

| snap file | change type | intended? | rationale |
|-----------|-------------|-----------|-----------|
| (none modified) | N/A | N/A | N/A |

### blockers

none.

---

## summary

all tests pass. all behaviors covered. no skips. no blockers.

- types: ✅
- lint: ✅
- format: ✅
- unit: 14/14 ✅
- integration: 38/38 ✅
