# execution: after-plan apply hint

## todos

- [x] create transformer `asApplyCommandFromArgv` in src/contract/cli/
- [x] add apply hint output to plan.ts after summary
- [x] write tests for transformer (10 cases, all pass)
- [x] verify output format matches vision

## progress

### done

1. created `asApplyCommandFromArgv.ts` transformer
   - detects invocation prefix (npx, pnpm dlx, yarn dlx, bare)
   - transforms plan args to apply args
   - removes --wish, --into, --snap, and passthrough args
   - preserves other user flags

2. updated `plan.ts`
   - captures original argv before passthrough arg injection
   - outputs apply hint after summary

3. wrote `asApplyCommandFromArgv.test.ts`
   - 10 test cases, all pass
   - covers npx, pnpm, yarn, bare invocations
   - covers flag removal and preservation

4. verified all tests pass
   - unit tests: 10/10 pass
   - integration tests: 22/22 pass
   - type check: pass
   - lint: pass

