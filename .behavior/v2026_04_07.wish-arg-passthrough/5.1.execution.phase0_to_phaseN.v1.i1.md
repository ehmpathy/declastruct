# execution: arg passthrough

> **root motto:** don't let declastruct interfere with --arg passthrough

---

## progress

- [ ] phase 0: test fixture
- [ ] phase 1: plan command changes
- [ ] phase 2: invoke command changes
- [ ] phase 3: apply command verification
- [ ] phase 4: documentation

---

## phase 0: test fixture

**status:** in progress

- [ ] create `src/.test/assets/wish-with-args.fixture.ts`
  - [ ] exports `getResources()` that parses `--env` from process.argv
  - [ ] exports `getProviders()` that returns demo provider
  - [ ] uses `parseArgs` from `util` with `strict: false`
  - [ ] defaults `env` to `'test'` when not provided
