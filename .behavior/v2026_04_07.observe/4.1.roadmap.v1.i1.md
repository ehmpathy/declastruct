# roadmap: declastruct --snap

execution roadmap for the blueprint in `.behavior/v2026_04_07.observe/3.3.1.blueprint.product.v1.i1.md`.

---

## phase 0: setup

### prereqs
- [ ] read `.behavior/v2026_04_07.observe/3.3.1.blueprint.product.v1.i1.md` (blueprint)
- [ ] read `.behavior/v2026_04_07.observe/1.vision.stone` (vision)
- [ ] read `.behavior/v2026_04_07.observe/2.1.criteria.blackbox.stone` (criteria)

### acceptance
- [ ] understand the output shape (observedAt, remote[], wished[])
- [ ] understand time of collection: snapshot collected BEFORE omitReadonly
- [ ] understand terminology: "resource" vs "desiredState" for wished[]

---

## phase 1: domain object

### prereqs
- [ ] read `.behavior/v2026_04_07.observe/3.1.3.research.internal.product.code.prod._.v1.stone` (prod patterns)

### tasks
- [ ] create `src/domain.objects/DeclastructSnapshot.ts`
  - [ ] define `DeclastructSnapshot` interface
  - [ ] define `DeclastructSnapshotEntry` interface
  - [ ] add .what/.why JSDoc headers

### acceptance
- [ ] `npm run test:types` passes
- [ ] DeclastructSnapshot has: observedAt, remote[], wished[]
- [ ] DeclastructSnapshotEntry has: forResource.class, forResource.slug, state

### verification
```sh
npm run test:types
```

---

## phase 2: planChanges update

### prereqs
- [ ] phase 1 complete
- [ ] read `.behavior/v2026_04_07.observe/3.1.3.research.internal.product.code.prod._.v1.stone` (prod patterns)

### tasks
- [ ] update `src/domain.operations/plan/planChanges.ts`
  - [ ] import DeclastructSnapshot, DeclastructSnapshotEntry
  - [ ] collect snapshot entry BEFORE omitReadonly
  - [ ] use `serialize(remoteState)` for remote[].state (may be null)
  - [ ] use `serialize(resource)` for wished[].state (always the declared resource)
  - [ ] use `getUniqueIdentifierSlug()` with error wrap for forResource.slug
  - [ ] return `{ plan, snapshot }` tuple
  - [ ] update return type signature

### acceptance
- [ ] `npm run test:types` passes
- [ ] planChanges returns `{ plan, snapshot }` tuple
- [ ] snapshot.remote[] contains serialized remoteState (null if new)
- [ ] snapshot.wished[] contains serialized resource (never null)
- [ ] snapshot collected BEFORE computeChange (before omitReadonly)

### verification
```sh
npm run test:types
```

---

## phase 3: plan.ts update

### prereqs
- [ ] phase 2 complete

### tasks
- [ ] update `src/contract/cli/plan.ts`
  - [ ] add `snapFilePath` parameter to executePlanCommand
  - [ ] destructure `{ plan, snapshot }` from planChanges result
  - [ ] write snapshot.json if snapFilePath provided
  - [ ] follow mkdir + writeFile pattern from plan.json write

### acceptance
- [ ] `npm run test:types` passes
- [ ] executePlanCommand accepts snapFilePath parameter
- [ ] snapshot written only when snapFilePath provided
- [ ] snapshot write follows mkdir + writeFile pattern

### verification
```sh
npm run test:types
```

---

## phase 4: invoke.ts update

### prereqs
- [ ] phase 3 complete

### tasks
- [ ] update `src/contract/cli/invoke.ts`
  - [ ] add `.option('--snap <file>', 'snapshot file path')` to plan command
  - [ ] pass snapFilePath to executePlanCommand

### acceptance
- [ ] `npm run test:types` passes
- [ ] `--snap` flag available on plan command
- [ ] flag value passed to executePlanCommand

### verification
```sh
npm run test:types
npx tsx ./bin/run plan --help | grep snap
```

---

## phase 5: integration tests

### prereqs
- [ ] phases 1-4 complete
- [ ] read `.behavior/v2026_04_07.observe/3.1.3.research.internal.product.code.test._.v1.stone` (test patterns)
- [ ] read `.behavior/v2026_04_07.observe/2.1.criteria.blackbox.stone` (criteria)

### tasks
- [ ] update `src/contract/cli/plan.integration.test.ts`
  - [ ] 'should create snapshot when --snap flag provided'
  - [ ] 'should not create snapshot when --snap flag absent'
  - [ ] 'snapshot should contain observedAt timestamp'
  - [ ] 'snapshot should contain remote[] with full state'
  - [ ] 'snapshot should contain wished[] with declared state'
  - [ ] 'snapshot entry should have forResource with class and slug'
  - [ ] 'snapshot state should contain _dobj stamp'
  - [ ] 'snapshot remote[].state should be null for new resources'
  - [ ] 'snapshot wished[].state populated for del() resources'
  - [ ] 'snapshot structure matches expected format' (snapshot assertion)

### acceptance
- [ ] all tests pass
- [ ] test coverage for positive, negative, and edge cases
- [ ] snapshot assertion captures output format

### verification
```sh
npm run test:integration -- src/contract/cli/plan.integration.test.ts
```

---

## phase 6: full verification

### prereqs
- [ ] phases 1-5 complete

### tasks
- [ ] run full test suite
- [ ] verify manual execution

### acceptance
- [ ] `npm run test` passes
- [ ] manual: `npx tsx ./bin/run plan --wish ... --into plan.json --snap snapshot.json` creates snapshot.json
- [ ] manual: snapshot.json contains observedAt, remote[], wished[]

### verification
```sh
npm run test
```

---

## dependency graph

```
phase 0 (setup)
    │
    v
phase 1 (domain object)
    │
    v
phase 2 (planChanges)
    │
    v
phase 3 (plan.ts)
    │
    v
phase 4 (invoke.ts)
    │
    v
phase 5 (tests)
    │
    v
phase 6 (verification)
```

phases are sequential — each depends on the prior phase to be complete.
