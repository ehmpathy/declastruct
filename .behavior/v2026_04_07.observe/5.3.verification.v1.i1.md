# verification checklist

## behavior coverage

| behavior | test file | status |
|----------|-----------|--------|
| --snap flag creates snapshot file | src/contract/cli/plan.integration.test.ts | ✅ |
| snapshot contains observedAt timestamp | src/contract/cli/plan.integration.test.ts | ✅ |
| snapshot contains remote[] with full state | src/contract/cli/plan.integration.test.ts | ✅ |
| snapshot contains wished[] with declared state | src/contract/cli/plan.integration.test.ts | ✅ |
| remote[].state is null for new resources | src/contract/cli/plan.integration.test.ts | ✅ |
| snapshot not created when --snap absent | src/contract/cli/plan.integration.test.ts | ✅ |
| snapshot scope matches plan scope | src/contract/cli/plan.integration.test.ts | ✅ |

## zero skips verified

- [x] no .skip() or .only() found in changed test files (grep found 0 matches)
- [x] no silent credential bypasses
- [x] no prior failures carried forward

## snapshot coverage for contract outputs

| contract | output variants | snapshot file | status |
|----------|-----------------|---------------|--------|
| declastruct plan --snap | success with snapshot | (inline assertions) | ✅ |

note: snapshot structure verified via inline assertions in plan.integration.test.ts

## snapshot change rationalization

no `.snap` files were modified by this PR.

| snap file | change type | intended? | rationale |
|-----------|-------------|-----------|-----------|
| (none) | - | - | - |

## contract output snapshot exhaustiveness

| contract type | contract | positive path snapped? | negative path snapped? | edge cases snapped? |
|---------------|----------|------------------------|------------------------|---------------------|
| cli | declastruct plan --snap | ✓ (inline assertions) | n/a (error paths tested via assertions) | ✓ (null state for new resource) |

note: --snap is an additive flag to extant `plan` command. error paths for the base command are covered by extant tests. snapshot structure verified via inline object assertions in integration tests.

## tests executed - with proof

| test suite | command run | result | proof |
|------------|-------------|--------|-------|
| types | npm run test:types | ✅ | exit 0, no errors |
| lint | npm run test:lint | ⚠️ | biome passed; depcheck failed on pre-extant rhachet-* deps |
| format | npm run test:format | ✅ | exit 0, 56 files checked |
| unit | THOROUGH=true npm run test:unit | ✅ | exit 0, 83 tests passed, 15 suites, 2 snapshots |
| integration | THOROUGH=true npm run test:integration | ✅ | exit 0, 65 tests passed, 6 suites |

## blockers

- lint depcheck: pre-extant rhachet-* dev dependencies flagged as unused (not introduced by this PR)
